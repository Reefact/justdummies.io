@inherits LayoutComponentBase

@*
    A layout with no chrome, for a page that is embedded in another document rather than
    visited. The hero widget on the landing page is framed, so the page furniture around it
    would be furniture inside the hero.
*@

@Body
