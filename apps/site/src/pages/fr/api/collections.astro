---
import CollectionsContent from '../../../components/api/CollectionsContent.astro';
import ApiPage from '../../../layouts/ApiPage.astro';
import { useTranslations } from '../../../i18n/ui';
import { site } from '../../../site';

const t = useTranslations('fr');
---

<ApiPage title={`${t('api.collections.heading')} — API — ${site.name}`} description={t('api.meta.description')} activeSlug="collections">
    <CollectionsContent />
</ApiPage>
