---
import EntryPointContent from '../../../components/api/EntryPointContent.astro';
import ApiPage from '../../../layouts/ApiPage.astro';
import { useTranslations } from '../../../i18n/ui';
import { site } from '../../../site';

const t = useTranslations('fr');
---

<ApiPage title={`${t('api.entryPoint.heading')} — API — ${site.name}`} description={t('api.meta.description')} activeSlug="entry-point">
    <EntryPointContent />
</ApiPage>
