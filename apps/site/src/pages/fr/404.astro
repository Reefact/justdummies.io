---
import BrandHeading from '../../components/BrandHeading.astro';
import Base from '../../layouts/Base.astro';
import { useTranslations } from '../../i18n/ui';
import { site } from '../../site';

/** The French twin of /404.html. See the note there. */
const t = useTranslations('fr');
---

<Base title={`${t('notfound.title')} — ${site.name}`} description={t('notfound.body')} noindex>
    <main class="shell">
        {/* The brand as the `h1`, and the refusal as the `h2` under it. See the English twin. */}
        <header class="brand">
            <BrandHeading />
        </header>

        <h2>{t('notfound.title')}</h2>
        <p class="body">{t('notfound.body')}</p>

        {/* Empty `alt` and intrinsic dimensions, for the reasons written out on the English
            twin. Same file: the mark belongs to the product, not to one language. */}
        <img class="crash" src="/404.png" alt="" width="1253" height="637" decoding="async" />

        <p><a href="/fr/">{t('notfound.home')}</a></p>
    </main>
</Base>

<style>
    .shell {
        max-width: 60rem;
        margin: 0 auto;
        padding: var(--jd-space-16) var(--jd-space-4);
    }

    .brand {
        margin-bottom: var(--jd-space-8);
    }

    h2 {
        font-size: var(--jd-text-xl);
        line-height: var(--jd-line-tight);
        margin: 0 0 var(--jd-space-4);
    }

    .body {
        color: var(--jd-text-muted);
        max-width: 46rem;
    }

    /* See the English twin. */
    .crash {
        display: block;
        width: 100%;
        max-width: 36rem;
        height: auto;
        margin: var(--jd-space-8) 0;
    }
</style>
