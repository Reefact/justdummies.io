---
import DocsIndexContent from '../../components/DocsIndexContent.astro';
import Page from '../../layouts/Page.astro';
import { useTranslations } from '../../i18n/ui';
import { site } from '../../site';

const t = useTranslations('en');
---

<Page title={`${t('docs.heading')} — ${site.name}`} description={t('docs.meta.description')}>
    <DocsIndexContent locale="en" />
</Page>
