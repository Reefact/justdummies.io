---
import UrisContent from '../../components/api/UrisContent.astro';
import ApiPage from '../../layouts/ApiPage.astro';
import { useTranslations } from '../../i18n/ui';
import { site } from '../../site';

const t = useTranslations('en');
---

<ApiPage title={`${t('api.uris.heading')} — API — ${site.name}`} description={t('api.meta.description')} activeSlug="uris">
    <UrisContent />
</ApiPage>
