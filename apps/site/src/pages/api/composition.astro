---
import CompositionContent from '../../components/api/CompositionContent.astro';
import ApiPage from '../../layouts/ApiPage.astro';
import { useTranslations } from '../../i18n/ui';
import { site } from '../../site';

const t = useTranslations('en');
---

<ApiPage title={`${t('api.composition.heading')} — API — ${site.name}`} description={t('api.meta.description')} activeSlug="composition">
    <CompositionContent />
</ApiPage>
